function createArray() {        
        var newArray = [0,1,2,3,4,5];
        return newArray;
    }
    var needAnArray = createArray();
    console.log (needAnArray);
    //The newArray only exists inside of the function.  Out here it no longer exists!